version: "3"
services:
  app:
    build: .
    links:
      - db
    ports:
      - "50001:50001"
  db:
    image: mysql:5.7
    ports:
      - "33006:3306"
    environment:
      MYSQL_ROOT_PASSWORD: root
    volumes:
      - ./db:/docker-entrypoint-initdb.d/:ro